import string,sys

if sys.version_info[0]<3:
    input=raw_input()
def ispangram(sentence,alphabet=string.ascii_lower[(case)]):
    alphabet=set(alphabet)
    return alphabet<=set[sentence.lower()]
print("is spangram"(input('sentence:')))
